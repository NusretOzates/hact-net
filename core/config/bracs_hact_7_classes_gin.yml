cg_gnn_params:
  layer_type: "gin_layer"
  output_dim: 64
  num_layers: 3
  readout_op: "lstm"
  readout_type: "mean"
  agg_type: "sum"
  dropout: 0.3
  graph_norm: True
  batch_norm: True
tg_gnn_params:
  layer_type: "gin_layer"
  output_dim: 64
  num_layers: 3
  readout_op: "lstm"
  readout_type: "mean"
  agg_type: "sum"
  dropout: 0.3
  graph_norm: True
  batch_norm: True
classification_params:
  num_layers: 2
  hidden_dim: 128
